import { ObservableStore } from './ObservableStore';
export declare class LocalStorageStore extends ObservableStore {
    private _storageKey;
    constructor(opts?: {
        storageKey?: string;
    });
    _getState(): any;
    _putState(newState: Record<string, unknown>): void;
}
