import { ObservableStore } from './ObservableStore';
export declare class MergedStore extends ObservableStore {
    private _children;
    constructor(children?: never[]);
    _addChild(child: ObservableStore): void;
    _updateWholeState(): void;
}
