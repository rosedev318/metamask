/// <reference types="node" />
import { Duplex as DuplexStream } from 'stream';
import { ObservableStore } from './ObservableStore';
declare class ObservableStoreStream extends DuplexStream {
    handler: (s: Record<string, unknown>) => void;
    obsStore: ObservableStore;
    constructor(obsStore: ObservableStore);
    pipe<T extends NodeJS.WritableStream>(dest: T, options?: {
        end?: boolean;
    }): T;
    _write(chunk: any, _encoding: string, callback: (error?: Error | null) => void): void;
    _read(_size: number): undefined;
    _destroy(err: Error | null, callback: (error: Error | null) => void): void;
}
export declare function storeAsStream(obsStore: ObservableStore): ObservableStoreStream;
export {};
